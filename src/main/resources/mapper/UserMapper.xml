<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.movie.mapper.UserMapper">
    <select id="queryUserByAccount" resultType="User">
        select * from users where account = #{account}
    </select>
    <insert id="insertUser" parameterType="User">
        INSERT INTO users (account, password,register_time)
        VALUES (#{account}, #{password},#{registerTime})
    </insert>
    <update id="updateLocation" parameterType="User">
        UPDATE users SET longitude = #{longitude} ,latitude = #{latitude}
        WHERE account = #{account}
    </update>
    <update id="updateUserInfo" parameterType="User">
        UPDATE users SET password = #{password},avatar = #{avatar},sex = #{sex},age = #{age},description = #{description}
        WHERE account = #{account}
    </update>
    <select id="getUsersWithinFiveKilometers" parameterType="map" resultType="com.example.movie.entity.User">
        SELECT u.*
        FROM users u
        JOIN member m ON u.account = m.account
        WHERE m.movie_id = #{movie_id}
        AND ST_Distance_Sphere(Point(u.longitude, u.latitude), Point(#{longitude}, #{latitude})) &lt;= 5000;
    </select>
</mapper>